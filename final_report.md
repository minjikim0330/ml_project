# 최종 보고
- Team 이인, 이연우, 김장원, 김민지
## 1. 프로젝트 소개
- 본 프로젝트는 날씨 데이터를 분석하여 기온과 체감온도 등의 요소를 반영한 옷차림을 추천하는 시스템을 구현한다.
- 또한 날씨에 따른 악세사리를 추천해주고, 성별 요소를 고려해 맞춤형 추천 기능을 통해 실생활에서 활용 가능한 서비스를 제안한다.
## 2. 프로젝트 배경
- 실제 경험을 통한 아이디어: 날씨에 맞는 옷차림을 선택하는 것은 일상에서 자주 겪는 고민 중 하나이다. 특히 계절이 바뀌는 시기나 일교차가 큰 날에는 현재 기온만 보고 옷을 선택했다가 덥거나 추운 경우가 있었다. 또한 같은 기온이라도 체감 온도, 강수 여부에 따라 적절한 옷차림이 달라질 수 있다는 접에 주목하였다
- 이에 사용자가 날씨 정보를 입력하면 상황에 맞는 옷차림과 추가 용품을 추천해 주는 시스템을 제작하고자 하였다. 본 프로젝트를 통해 일상생활에서 활용 가능한 서비스를 구현하고자 하였다.
## 3. 데이터셋 소개
### 📊 데이터셋 및 특징 (Dataset Summary)

| 데이터셋 (Source) | 수집 변수 (Original Features) | 데이터 형태 및 특징 (Description) |
| :--- | :--- | :--- |
| **GitHub Open Repository** | `temp` (기온), `weather` (날씨), `outer`, `inner`, `accessories` 등 | 정량적인 기상 조건과 표준 의류 가이드가 매칭된 기준 데이터 (10000건) |
| **Instagram Crawling Data** | `gender` (성별), `style_tags`, 텍스트 패션 데이터 등 | 실제 유저들의 성별 맞춤형 트렌드 및 비정형 의류 스타일 데이터 (100건) |
## 4. 전처리 과정
- 클라우드 기반 데이터 가상 연동 (Drive Mount): google.colab.drive 모듈을 활용하여 구글 드라이브 내 저장된 비정형 인스타그램 크롤링 데이터셋을 코랩 개발 환경과 실시간 동적 연동함.
- 한국어 인코딩 최적화: 인스타그램 크롤링 파일 로드 시 발생하는 한글 깨짐 및 디코딩 예외를 방지하기 위해 encoding='cp949' 옵션을 적용하여 데이터 무결성을 확보함.
- 결측 특징 생성 및 구조 동질화 (Feature Matching): 두 데이터셋의 통합 합병을 위해 인스타그램 데이터셋에 누락된 outer 및 inner 컬럼을 신규 생성하고 기본값('없음')으로 초기화하여 스키마를 일치시킴.
- 컬럼명 표준화 및 매핑 오류 방지: 데이터 셋 간 상이한 특징 이름(단수형 accessory $\rightarrow$ 복수형 accessories)을 if-rename 조건문으로 제어하여 병합 시 데이터 유실 및 매핑 에러를 사전 방지함.
- 수학적 구간 분기 알고리즘을 통한 타겟 라벨링 (Target Labeling): 날것의 기온(temp) 데이터를 의류 추천 목적에 맞게 4단계 카테고리 변수(outfit_level)로 변환하는 assign_outfit_level 커스텀 함수 구축. 겨울 한파(Level 0, 4°C 이하)부터 더운 여름(Level 3, 23°C 이상)까지 예외 없는 명확한 규칙 기반 정답지($y$) 창조.
- 범주형 데이터 변환 (Feature Encoding): 문자열로 된 독립 변수(weather, gender)와 종속 변수(outfit_level)를 AI 모델이 연산할 수 있도록 사이킷런의 LabelEncoder를 활용하여 정수형(Integer) 데이터(weather_encoded, gender_encoded, target)로 최종 인코딩함.
## 5. 모델 구조
- - 알고리즘: 랜덤 포레스트 (Random Forest Classifier)
  - 입력 데이터 (Features, $X$): 인코딩된 기온 수치(temp), 날씨 변수(weather_encoded), 성별 변수(gender_encoded)
  - 출력 데이터 (Target, $y$): 최종 예측된 의류 두께감 정수형 라벨 (target $\rightarrow$ 역인코딩을 통해 Level 0~3 매칭)
  - 작동 메커니즘: 본 프로젝트에서는 데이터의 비선형적 관계를 정교하게 학습하기 위해 앙상블(Ensemble) 기법의 대표 주자인 랜덤 포레스트(Random Forest)를 채택함.
  - 입력된 기온, 날씨, 성별 데이터를 바탕으로 여러 개의 독립적인 의사결정나무(Decision Tree)들을 무작위로 생성함.
  - 각 나무들이 학습 데이터의 다양한 측면을 독립적으로 분석하여 "이 기온과 성별에는 몇 단계 두께감이 적절하다"는 결과를 각각 예측함
  - 최종적으로 모든 의사결정나무들이 내놓은 예측치들을 모아 다수결 투표(Majority Voting) 방식으로 최종 outfit_level을 결정함.
  - 이 방식은 단일 의사결정나무의 단점인 과적합(Overfitting) 문제를 완벽히 방지하고, 성별이나 날씨 같은 범주형 변수와 기온이라는 연속형 변수가 섞여 있는 환경에서 매우 안정적이고 높은 예측 정확도를 보여줌.
  ## 6. 레퍼런스 개선점
  - 단일 추천 구조에서 성별 기반 데이터 분리를 통한 개인화 정교화
  - 기존 문제점 (1차 기획): 초기에는 성별 구분 없이 모든 유저에게 모두 같은 옷차림을 추천하는 구조를 구상함. 남성에게 치마가 추천되는 경우가 생김.
  - 진행 중 개선점 (최종): 인스타그램 크롤링 데이터셋에서 성별 변수(gender)를 완벽히 분리하고 이를 모델의 핵심 독립 변수로 추가 학습시킴. 그 결과, 동일한 기온 조건에서도 남성과 여성에게 각각 차별화된 맞춤형 하의 및 스타일링 파생 변수를 매칭하는 '개인화 알고리즘'으로 성능을 대폭 개선함.
  - 의류 추천을 넘어선 날씨 맞춤형 액세서리(Accessories) 확장 추천
  - 기존 문제점 (1차 기획): 초기 모델은 상의, 하의, 아우터 등 기본적인 옷차림 추천에만 치중하여, 기상 상황에 따른 실질적인 유저 편의성을 제공하기엔 다소 밋밋하다는 아쉬움이 있었음.
  - 진행 중 개선점 (최종): 데이터셋 전처리 과정에서 컬럼명을 표준화하는 것에 그치지 않고, 날씨(weather) 변수와 액세서리(accessories) 데이터를 연동하는 서브 규칙을 추가함. 예를 들어, 날씨가 '비(Rainy)'일 때는 우산(Umbrella)을, '맑고 자외선이 강함(Sunny)'일 때는 선글라스(Sunglasses)나 모자를 함께 추천 목록에 바인딩(Binding)해 주는 복합 추천 레이어를 추가하여 서비스의 실용성을 극대화함.
  ## 7. 프로젝트 결과
  - ### 랜덤 포레스트 기반 고정밀 의류 추천 모델 확보
  - 기온(temp), 날씨(weather_encoded), 성별(gender_encoded)의 복합적인 상관관계를 학습하는 랜덤 포레스트(Random Forest) 분류 모델을 성공적으로 구현함.
  - 다양한 기상 변수와 범주형 변수가 혼합된 환경에서도 안정적이고 신뢰도 높은 의류 두께감(outfit_level, Level 0~3) 예측 결과를 도출함.
  - ### 성별 데이터 분리를 통한 '개인화 서비스' 실현
  - 동일한 기온과 날씨 조건이라도 사용자의 성별에 따라 완전히 차별화된 하의 및 스타일링 파생 변수가 매칭되도록 모델을 고도화함.
  - ### 기상 조건 연동형 액세서리(Accessories) 확장 추천 성공
  - 상·하의 위주의 기본 의류 추천 레이어에 더해, 날씨 변수(맑음, 비 등)에 따라 실용적인 액세서리를 함께 바인딩하는 매핑 규칙을 성공적으로 적용함.
  - 예컨대 우산, 선글라스, 모자 등 기상 상황에 즉각적으로 대응할 수 있는 소품 추천을 결합하여, 기존 시스템 대비 서비스의 실용성과 완성도를 대폭 끌어올림
  - ## 8. 추후 발전 방향
  - ### 실시간 날씨 OpenAPI 연동을 통한 동적 추천 시스템 고도화
  - 현재는 수집된 고정 데이터셋(CSV) 환경에서 모델 학습 및 예측을 수행한다는 한계가 있음.
  - 추후 기상청 공공데이터포털 또는 OpenWeatherMap의 실시간 날씨 OpenAPI를 연동하여, 사용자의 현재 위치(GPS) 기준 실시간 기온, 강수 플래그, 미세먼지 수치 등을 동적으로 파이프라인에 수집하고 즉각적인 옷차림을 제안하는 실시간 스트리밍 서비스로 확장하고자 함.
  - ### LLM(대형 언어 모델) 융합을 통한 패션 큐레이션 챗봇 및 비주얼 가이드 구현
  - 현재 텍스트 기반으로 출력되는 단순 의류 레벨 및 아이템 추천 레이어를 넘어, ChatGPT나 Claude 같은 LLM API를 모델의 아웃풋 단에 결합하고자 함.
  - "오늘 22도인데 데이트 룩으로 자켓 어때?"와 같은 유저의 자연어 질문을 처리하는 생성형 패션 챗봇 인터페이스를 구축하고, 인스타그램 이미지 크롤링 데이터와 연동해 실제 스타일링 사진까지 카드 뉴스 형태로 시각화하는 '인공지능 패션 스타일리스트'로 고도화.
  - ## 9. 역할분담
  - - 이 인: 데이터 크롤링, 웹 인터페이스 고도화
    - 이연우: 자체 수집 데이터 제작, KNN/ LightGBM 모델 성능 평가
    - 김장원: 모델 추천, 발표 자료 제작
    - 김민지: 데이터 전처리, 코드 구현, 보고서 작성
