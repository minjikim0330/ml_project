# 최종 보고
## 데이터 설정
### 데이터 구성 및 생성 - 연우님
본 프로젝트에서는 날씨 기반 옷 추천을 위한 학습 데이터를 구성하였다.  
데이터는 실제 기온(temp), 체감온도(feel_temp), 날씨(weather), 성별(gender)을 기준으로 아우터, 상의, 하의, 이너웨어, 추가용품(accessories)을 추천하도록 설계하였다.
또한 성별에 따라 하의 데이터를 구분하여 남성 사용자에게 치마가 추천되지 않도록 구성하였으며, 날씨 환경에 따라 추가용품 추천이 가능하도록 데이터를 생성하였다.
- 추운 날씨: 목도리, 장갑, 핫팩 등 방한용품 추천
- 더운 날씨: 선글라스, 모자, 양산 등 여름용품 추천
- 비 오는 날: 우산, 레인부츠 추천
- 눈 오는 날: 부츠, 장갑 등 겨울용품 추천
데이터는 Python과 ChatGPT를 활용하여 CSV 형태의 학습 데이터셋으로 생성하였으며, 다양한 날씨 상황을 반영하기 위해 총 100개, 1000개, 10000개 규모의 데이터셋을 구성하였다.
### 크롤링 - 인님
## 모델 설정
### 모델 선택 및 이유 - 장원님
1.KNN (100개 미만 용)
  - 데이터셋이 100개 미만인 초기 단계에서 가장 안정적인 결과를 제공합니다.
  - 복잡한 학습 과정 없이도 사람의 인지 방식과 유사한 상식적인 추천이 가능하며 구현이 매우 쉽습니다.

2. 랜덤 포레스트 (1000개 규모)

  -데이터가 200개에서 수천 개로 늘어남에 따라, 변수가 많아져도 성능이 저하되지 않는 모델입니다.
  
  -기온 외에 습도, 강수 여부 등 조건이 추가 되어도 특정 데이터에만 치우치는 과적합없이 높은 예측 정확도를 유지합니다.

4. LightGBM (10000개 이상)
  - 10000개 이상의 대규모 데이터셋에서 압도적인 학습 속도와 정밀한 성능을 확보하기위해 채택했습니다.
  - 앞선 모델이 틀린 부분을 다음 모델이 집중적으로 학습하며 이 방식 덕분에 다른 모델보다 훨씬 정교하한 예측이 가능합니다.
## 코드
### 1. 데이터 수집 및 전처리
본 프로젝트의 예측 정확도와 스타일링 추천의 다양성을 확보하기 위해 다중 소스 데이터를 수집하고 이를 통합하는 파이프라인을 구축하였다.
#### 다중 데이터 소스 연동
- 직접 수집한 날씨별 의류 매칭 데이터와 인스타그램 등 SNS에서 크롤링한 실생활 패션 트렌드 데이터를 결합하였다.
#### 데이터 보존성 및 환경 최적화 
- 코랩의 세션 초기화 문제를 해결하기 위해 구글 드라이브 마운트 방식을 도입하였으며, 세션 재연결 시에도 데이터가 안정적으로 유지되도록 했다.
- 데이터 내 한국어 인코딩 오류를 방지하기 위해 파일 시스템에 맞춰 규칙 기반의 데이터 로드 메커니즘을 적용하여 전처리 효율을 극대화 했다.
### 2. 머신러닝 기반 의류 레벨 예측 모델 학습
#### 알고리즘 선정
- 복잡한 기상 변수 간의 비선형적 관계를 효과적으로 학습하고 과적합을 방지하기 위해 앙상블 기반의 랜덤 포레스트 모델을 사용하였다.
#### 독립변수
  - 기온, 체감온도, 날씨, 성별
#### 종속변수
  - 데이터셋 내 정의된 의류 추천 레벨
#### 학습 및 검증
- 전체 데이터셋을 학습 데이터와 테스트 데이터로 8:2로 분할하여 교차 검증을 수행했고, 정밀도와 재현율 측면에서 매우 안정적인 성능 수치를 확인했다.
### 사용자 맞춤형 데이터 분기 및 분류 로직 설계
#### 성별 기반 의류 매칭
- 모델이 기온에 맞는 의류 레벨을 판정하면, 프로그램이 사용자의 성별을 감지하여 추천 아이템 목록을 실시간으로 자동 전환합니다. 같은 날씨여도 성별에 따라 맞춤형으로 코디가 매칭되게 설계했다.
#### 다중 기상 조건 반영 악세사리
- 단순히 기온에 의지하는 것을 넘어 강수 상태를 결합하여 우산, 레인부츠, 선글라스 등 날씨에 따른 필수 악세사리를 추가 제안하는 서브 로직을 구축했다.
### 확률 기반 스타일링 매칭 알고리즘
- 매번 고정된 결과만을 제공하는 시스템의 한계를 극복하고 서비스의 실용성과 다양성을 높이기 위해 확률적 무작위 추출 알고리즘을 결합했다.
#### 독립적 아이템 셔플
- 분류된 성별 및 날씨 카테고리 내부에서 아우터, 상의, 하의 데이터 리스트를 각각 독립적인 배열로 분리하여 정의했다.
#### random.choice 메커니즘 활용
- 사용자가 시스템을 사용할 때마다 각 의류 카테고리 배열에서 아이템을 독립적으로 추출하도록 했다.
#### 기대효과
- 제한된 패션 데이터셋 안에서도 상하의 및 아우터의 조합 가짓수를 늘려서, 사용자가 접속할 때마다 항상 새로운 형태의 코디를 추천받을 수 있도록 한다.

